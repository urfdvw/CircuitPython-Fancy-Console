{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "raw_console": {
            "type": "object",
            "properties": {
                "hide_title": {
                    "type": "boolean"
                },
                "hide_cv": {
                    "type": "boolean"
                }
            },
            "required": ["hide_title", "hide_cv"]
        }
    },
    "required": ["raw_console"]
}
